logging:
  level:
    org:
      aszjch:
        demoapp:
          infrastructure:
            config:
              MinIOContainerConnectionDetailsFactory: debug
              MinIOAutoConfiguration: debug
logging.level.org.aszjch.demoapp: debug
spring:
  jpa:
    hibernate:
      ddl-auto: create
  servlet:
    multipart:
      max-file-size: 10MB
  kafka:
    consumer:
      group-id: test
    admin:
      auto-create: true
    template:
      default-topic: "articles"